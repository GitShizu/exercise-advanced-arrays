// Crea una funzione chiamata logElements che prende un array e stampa in console ogni elemento utilizzando il metodo .forEach().

const logElements = (array)=>{
    array.forEach(element => console.log(element));
}

// Test
// const testArray = ['Gianny', 'Serjoh', 'Valenteenah'];
// logElements(testArray);